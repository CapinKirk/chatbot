{
  "name": "@chat/prisma",
  "private": true,
  "scripts": {
    "generate": "prisma generate",
    "migrate": "prisma migrate dev --name init",
    "reset": "prisma migrate reset --force --skip-generate && prisma generate && node seed.cjs"
  },
  "devDependencies": {
    "prisma": "6.13.0",
    "@prisma/client": "6.13.0"
  }
}
