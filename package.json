{
  "name": "chat-refactor",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@9.7.0",
  "scripts": {
    "dev": "pnpm -w -C services/api dev & pnpm -w -C services/ai-bot dev & pnpm -w -C services/directory-sync dev & pnpm -w -C apps/web dev & pnpm -w -C apps/admin dev",
    "build": "pnpm -w -r run build",
    "test": "pnpm -w -r run test",
    "lint": "pnpm -w -r run lint",
    "typecheck": "pnpm -w -r run typecheck",
    "e2e": "pnpm -w -C apps/web e2e",
    "db:reset": "pnpm -w -C prisma run reset",
    "load": "node -e \"console.log('Install k6 locally to run load: k6 run infra/k6/load.js')\""
  },
  "devDependencies": {
    "@prisma/client": "6.13.0",
    "prisma": "6.13.0"
  }
}
